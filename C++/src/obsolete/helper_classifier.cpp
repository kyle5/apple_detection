// include everything 
// opencv
cv::CvRTrees create_classifier(  ) {
			bool CvRTrees::train(const Mat& trainData, int tflag, const Mat& responses, const Mat& varIdx=Mat(), const Mat& sampleIdx=Mat(), const Mat& varType=Mat(), const Mat& missingDataMask=Mat(), CvRTParams params=CvRTParams() )

		C++: bool CvRTrees::train(const CvMat* trainData, int tflag, const CvMat* responses, const CvMat* varIdx=0, const CvMat* sampleIdx=0, const CvMat* varType=0, const CvMat* missingDataMask=0, CvRTParams params=CvRTParams() )

		C++: bool CvRTrees::train(CvMLData* data, CvRTParams params=CvRTParams() )

}

query_classifier( cv::CvRTrees random_forest, // <features_input ) {
	
}

